%%odpowiedz skokowa
kon = 200;
y_mod_step = zeros(kon, 1);
for i = 8:kon
    u_step(i) = 1;
    y_mod_step(i) = W_kon(1)*u_step(i-6) + W_kon(2)*u_step(i-7) + W_kon(3)*y_mod_step(i-1) + W_kon(4)*y_mod_step(i-2);
end

clf;
hold on;
plot(y_mod_step);
plot(u);
xlabel('k');
legend('y_{step}', 'u');
print('-dpng', '../images/z2.png');
hold off;